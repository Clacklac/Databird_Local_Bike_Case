version: 2

models:
  - name: mrt_seasonal_order_report
    description: '{{ doc("mrt_seasonal_order_report") }}'
    columns:
      - name : order_date
        description: 'order_date'
        tests: 
          - unique
          - not_null
