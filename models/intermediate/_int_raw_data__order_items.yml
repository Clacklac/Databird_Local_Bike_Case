version: 2

models:
  - name: int_raw_data__order_items
    description: '{{ doc("int_raw_data__order_items") }}'
    columns:
      - name: order_item_id
        description: "Primary key"
        tests:
          - unique
          - not_null